import React from "react";

// Loading message displayed while API data is being fetched

function Loading() {
  return (
    <h1 className="container text-center white">Loading ...</h1>
  )
}

export default Loading;